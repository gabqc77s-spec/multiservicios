[project]
name = "fire-service"
version = "0.1.0"
description = "A Python script to sum two numbers, with an initial syntax error."
authors = [
    { name = "Your Name", email = "your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
